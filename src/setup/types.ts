enum Department {
    id,
    title,
    slug,
    description,
    created,
    updated,
}

enum Course {
    id,
    courseID,
    departmentID,
    title, 
    credit,
    semester,
    studylevel,
    url,
    created,  
    updated,
}